import { Translation } from '../translation';

export const enTranslation: Translation = {
    toDoDescription: 'This is just a demo of TodoMVC for testing, not the real TodoMVC app.',
    toDoHeader: 'todos'
};
